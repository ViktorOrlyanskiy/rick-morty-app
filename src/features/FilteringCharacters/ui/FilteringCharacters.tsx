import { memo } from "react";

interface FilteringCharactersProps {}

export const FilteringCharacters: React.FC<FilteringCharactersProps> = memo(
    (props) => {
        const {} = props;
        return <div></div>;
    }
);
